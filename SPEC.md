# 初三语文中考备考 APP · 需求规格说明 v2

> 面向：常州市 2026 中考 · 初三下学期单人使用 · 本地原生安卓 App · 调用 DeepSeek API
> 范围：文言文（课内 / 课内外对比）+ 现代文阅读理解 · AI 出题 + AI 批改
> 本版目标：搭好整体框架，两大题型同时跑通最小闭环
> v2 新增：做题时间统计与时间评价（§7）、轻量即时激励机制（§8）

---

## 1. 政策与考情约束（出题/批改必须对齐）

| 项目 | 约束 | 来源 |
|---|---|---|
| 中考时间 | 2026 年 6 月 18—20 日 | 常州市 2026 招生意见 |
| 语文分值 | 120 分（总分 700） | 同上 |
| 试卷结构 | 积累与运用 20 分 + 文言文阅读 + 现代文阅读 + 作文，时长 150 分钟 | 常州近年真题 |
| 文言文特色 | **课内外对比阅读**（【甲】课内 +【乙】课外），约 15 分 | 常州真题 |
| 文言文考点 | 实词、虚词（之/而/于/以等）、断句、句子翻译、词类活用、通假字、内容理解/人物形象 | 课标+真题 |
| 现代文文体 | 记叙文/散文/小说、说明文、议论文 | 真题 |
| 现代文考点 | 主旨概括、词句含义与作用、人物形象、写作手法及作用、结构思路、情感态度 | 真题 |
| 教材依据 | 统编版（部编版），课内 22 篇核心文言文 | 教材 |

> ⚠️ **待校准**：常州每年单独下发《语文学科命题说明/考试说明》，含当年精确篇目、题型示例与分值表。拿到 2026 版后，把篇目清单与分值写入 App 配置，覆盖本表中由真题反推的部分。

### 课内 22 篇文言文（题库锚点，原文固定不可由 AI 生成）
《论语》十二章、《陋室铭》、《爱莲说》、《河中石兽》、《三峡》、《答谢中书书》、《记承天寺夜游》、《富贵不能淫》、《生于忧患，死于安乐》、《桃花源记》、《小石潭记》、《北冥有鱼》、《虽有嘉肴》、《马说》、《岳阳楼记》、《醉翁亭记》、《湖心亭看雪》、《鱼我所欲也》、《送东阳马生序》、《曹刿论战》、《邹忌讽齐王纳谏》、《出师表》

---

## 2. 核心原则：防止 AI「乱出题」（重点）

LLM 会编造不存在的"古文"。本 App 用**分层策略**保证文言文真实、命题合规：

### 文言文原文来源（三层，按可靠性排序）
1. **课内原文 = 本地固定语料库**。22 篇权威原文 + 注释 + 译文随 App 内置，AI **绝不**生成或改写课内原文，只能"读取 + 命题 + 批改"。
2. **课外对比篇 = 本地课外语料库（首选）**。你预先收录一批真实、浅易、与课内主题/虚词相通的课外短文（出处明确：《世说新语》《战国策》《史记》列传、唐宋笔记等），AI 从库中**选取**，再命题。
3. **AI 提议课外篇（兜底，带强校验）**。仅当库里没有合适篇目时启用，且必须满足全部硬条件：
   - 必须给出真实出处（书名 + 朝代 + 作者），不得标注"佚名/出处不详"；
   - 文本须可在传世文献中找到，禁止任何形式的原创/改写/仿写文言文；
   - 输出 `verified=false` 标记，**进入"待人工确认"状态**，未确认不得正式入题库。

### 现代文原文来源
- 现代文阅读理解**允许 AI 生成原创短文**（这是常规做法，不存在真实性约束），但要求文体、字数、难度对齐中考。

### 命题合规的通用约束（写进系统提示词）
- 题型、分值、采分点必须对齐第 1 节的考情表；
- 每题必须给：参考答案 + **采分点（踩点给分）** + 考查能力点；
- 不超纲、不出偏怪题；翻译题逐句给关键词得分点。

---

## 3. 技术架构（原生安卓 Kotlin）

| 层 | 选型 |
|---|---|
| UI | Jetpack Compose（推荐）+ MVVM |
| 异步 | Kotlin Coroutines + Flow |
| 网络 | Retrofit + OkHttp + Kotlinx Serialization |
| 本地存储 | Room（SQLite）|
| API | DeepSeek（OpenAI 兼容），默认 `deepseek-v4-pro` 思考模式，开启 JSON 输出；设置页可下拉切换（见客户端 DeepSeekApi.kt）|
| Key 管理 | 用户在设置页填入，存 EncryptedSharedPreferences，**不硬编码进 APK** |

> DeepSeek 接口为 OpenAI 兼容风格（`/chat/completions`，`response_format: {type:"json_object"}`）。具体字段以官方文档为准，开发时核对一次。

### 建议模块划分（package）
```
data/        Room实体、DAO、Repository、DeepSeek API、本地语料库loader
domain/      出题/批改的用例(UseCase)、提示词模板、JSON解析与校验
ui/          dashboard / setup / exercise / result / wrongbook / settings
core/        网络、序列化、错误处理、Key存取
```

---

## 4. 数据模型（Room 实体草案）

- **TextBank**：`id, category(课内/课外), title, author, dynasty, text, notes, translation, source_ref, theme, verified`
- **Exercise**：`id, type(文言对比/文言单篇/现代文), createdAt, configJson, payloadJson(出题快照), status, totalTimeSec(本次总用时)`
- **Question**：`id, exerciseId, qType, stem, refAnswer, scorePoints(JSON), maxScore, abilityTag`
- **Attempt**：`id, questionId, studentAnswer, gotScore, pointCheck(JSON), errorType, explanation, tip, timeSpentSec(本题用时), gradedAt`
- **WrongQuestion**：`id, questionId, abilityTag, addedAt, masteredFlag`
- **WeakPointStat**：`abilityTag, attempts, wrongCount, avgTimeSec(该考点平均用时), lastUpdated`
- **UserProgress**（单行）：`xp, level, levelTitle(称号), coins, streakDays, lastPracticeDate`
- **Badge**：`id, code(对应速度×准确率象限或里程碑), name, exerciseId, earnedAt`

---

## 5. DeepSeek 提示词模板（关键）

> 完整、最新的出题/批改提示词见《全部提示词_按顺序.md》附录 A（与 AiDtos.kt 字段严格对齐）。
> 要点：只输出 JSON、key 用 snake_case、采分点合计=该题满分、各题满分合计=总分；文言原文用我方提供的真实文本（防伪）；批改踩点给分；只取 message.content。


---

## 6. JSON 结构契约（App 与 AI 之间）

### 出题返回
```json
{
  "type": "wenyan_compare",
  "total_score": 15,
  "passage_in":  {"title":"","author":"","dynasty":"","text":"","notes":[]},
  "passage_out": {"title":"","author":"","dynasty":"","text":"","notes":[],
                  "source_ref":"","verified":true,"need_human_review":false},
  "questions": [
    {"id":"q1","qType":"实词解释","stem":"","refAnswer":"",
     "scorePoints":[{"point":"","score":1}],"maxScore":4,"abilityTag":"文言实词"}
  ]
}
```

### 批改返回（AI 部分）
```json
{
  "perQuestion": [
    {"id":"q1","gotScore":2,"maxScore":4,
     "pointCheck":[{"point":"","hit":true,"score":1}],
     "errorType":"","correctAnswer":"","explanation":"","tip":""}
  ],
  "totalGot": 0, "totalFull": 0,
  "weakPoints": [""],
  "nextAdvice": ""
}
```

### 本次结算（App 本地合成，非 AI 返回）
> 用时由 App 计时得到，时间评价由"用时×正确率"在端上计算（基准见 §7），与 AI 批改结果合并后展示。
```json
{
  "timeSpentSec": 0,
  "baselineRange": [960, 1200],
  "perQuestionTime": [{"id":"q1","sec":0}],
  "slowestQuestion": {"id":"q3","sec":0},
  "timeRating": "考场节奏",
  "accuracy": 0.0,
  "xpGained": 0, "coinsGained": 0,
  "badgeEarned": {"code":"fast_accurate","name":"神速准星"}
}
```
> App 端必须对返回做 schema 校验：字段缺失、`need_human_review=true`、`verified=false` 时拦截，提示重试或转人工确认，绝不直接入题。

---

## 7. 时间统计与评价

### 7.1 优秀学生用时基准（依据公开备考资料；随 2026 命题说明校准）
名师常用整卷"334"节奏（120 分钟卷示例）：前 30 分钟基础题、中间 50 分钟阅读、最后 40 分钟作文。落到本 App 两大类，单次练习基准如下（会按实际题量/分值线性归一化）：

| 题型 | 单次基准用时 | 优秀线 |
|---|---|---|
| 文言对比阅读（约15分·两篇） | 16–20 分钟 | ≤16 分钟 |
| 文言单篇 | 8–12 分钟 | ≤9 分钟 |
| 现代文 记叙/散文 | 18–25 分钟 | ≤20 分钟 |
| 现代文 说明/议论 | 12–18 分钟 | ≤14 分钟 |

> 归一化：以"基准用时 ÷ 本次总分"得到每分值基准秒数，再乘以实际生成题目的总分，得到本次基准区间，使不同题量的练习可比。

### 7.2 采集指标
- 本次总用时 `totalTimeSec`、单题用时 `timeSpentSec`、单题平均用时、最耗时题（卡点定位）。
- 计时规则：进入答题页开始计时；每题切换记录该题耗时；提交批改时停表。中途切到后台暂停计时。

### 7.3 评价维度（核心：速度 × 准确率，不单看快慢）
以"是否在基准内"× "正确率是否达标（默认 ≥80%）"分四象限给评语：

| | 用时达标 | 用时超时 |
|---|---|---|
| **正确率高** | 「考场节奏 · 又快又准」 | 「准度够 · 该提速」 |
| **正确率低** | 「求快丢分 · 放慢审题」 | 「基础待巩固 · 先求对再求快」 |

- 趋势：错题本/薄弱点页展示"近 N 次用时曲线"，观察是否越练越快。
- `WeakPointStat.avgTimeSec` 记录各考点平均用时，定位"又慢又错"的薄弱考点优先攻坚。

---

## 8. 轻量即时激励机制（克制版，仅 4 个机制）

> 原则：即时反馈、视觉+数值同步、带一点惊喜感；不做商城、不做复杂任务系统、不制造焦虑。

- **练前 · 目标卡**：选完范围弹一张小卡——"本次目标：正确率 ≥80% 且 用时 ≤18 分钟"，配"开始挑战"按钮，建立即时目标感。
- **练后 · 即时结算**：提交后立刻播放结算动画——得分 + 经验值(XP)进度条增长 + 积分(coins)跳动。
- **即时徽章（惊喜感）**：按 §7.3 四象限或里程碑盖一枚章（如"神速准星""稳扎稳打""连胜达人"），写入 `Badge` 表。
- **连续打卡 + 等级**：首页显示 `streakDays` 连续天数；XP 升级走称号体系（文言学徒 → 文言行家 → 文言高手；现代文同理），称号存 `UserProgress.levelTitle`。

激励数值建议（可在设置里调）：每题踩点得分 × 系数 = XP；达标练习额外奖励 XP；连续打卡每日小额积分加成。所有数值本地计算、本地存储。

---

## 9. 页面流（最小框架）

1. **首页 Dashboard**：今日练习入口、连续打卡 `streakDays` 与等级/称号、错题数、薄弱考点 Top3。
2. **出题设置**：选大类 → 文言（单篇/对比、篇目、难度、题型）｜现代文（文体、难度、题量）；进入答题前弹"目标卡"。
3. **答题页**：逐题作答并计时，文言题支持原文随时回看；可选专注模式（隐藏计时减压）。
4. **批改结果页**：逐题采分点对照 + 错因 + 正确答法 + 提升建议 + 总分 + 薄弱点；顶部播放结算动画（XP/积分/徽章），展示用时与时间评价（§7.3）。
5. **错题本**：按考点归类，含用时曲线，可"针对薄弱点再出一套"。
6. **设置**：DeepSeek API Key、模型、默认难度、激励数值与达标阈值。

---

## 10. 开发路线图（先搭框架，两类同跑）

**阶段 0 · 脚手架**
- 工程初始化、MVVM 骨架、Room 建库（含用时字段、UserProgress、Badge）、Retrofit 接通 DeepSeek、设置页存 Key、JSON 序列化与 schema 校验、答题计时基础设施。

**阶段 1 · 课内语料库**
- 录入 22 篇课内原文+注释+译文；做 1–2 篇课外短文进课外库（验证选篇逻辑）。

**阶段 2 · 出题闭环（两类）**
- 文言对比出题（库内选篇）+ 现代文出题，渲染答题页并计时。

**阶段 3 · 批改闭环**
- 踩点批改 + 结果页详解 + 提升建议 + 用时与时间评价（§7）。

**阶段 4 · 进步系统 + 激励**
- 错题本、薄弱点统计、用时曲线、按薄弱点再练；XP/等级/积分/徽章/连续打卡（§8）。

**阶段 5 · 加固**
- AI 提议课外篇的人工确认流；命题说明 2026 版校准（含篇目与时间基准）；异常/限流/离线处理。

---

## 11. 已知风险与对策

| 风险 | 对策 |
|---|---|
| AI 编造不存在的文言文 | 课内固定库、课外优先选库、AI 提议必带出处+人工确认 |
| 采分点不准 / 批改偏松偏严 | 提示词强制踩点给分；结果页允许学生人工复核与申诉记录 |
| API Key 泄露 | EncryptedSharedPreferences，绝不入代码/不上传 |
| 篇目范围与当年考纲不符 | 阶段 5 用 2026 命题说明校准，篇目做成可配置 |
| 断网 | 题库/错题本本地可用，仅出题/批改需联网并给出友好提示 |
