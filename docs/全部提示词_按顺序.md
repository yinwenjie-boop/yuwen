# 全部提示词 · 按顺序（一份搞定）

> 用法：从上往下，**一段一段复制**给 Claude Code。每段做完，让 Claude Code 跑 `./gradlew assembleDebug` 确认能编译通过，再发下一段。不要一次全发。
> 开发环境：VS Code + Claude Code 扩展；编译/打包交给 GitHub Actions 云端，本机不必装安卓 SDK。
> 配套文件：`SPEC.md`（需求）、7 个料文件（5 个 .kt/3 个 json）。料文件清单见《00_总说明》。

---

## 〔第 1 段〕阶段 0 · 搭骨架

```
你是资深 Android 工程师。我们做一个本地运行的原生安卓 App，给一名初三下学期学生备考常州市2026中考语文，调用 DeepSeek API 实现 AI 出题与 AI 批改，覆盖文言文与现代文阅读理解两大类。完整需求见仓库根目录 SPEC.md，开工前请先完整读一遍 SPEC.md，一切以它为准。

技术栈（固定）：Kotlin + Jetpack Compose + MVVM；Coroutines+Flow；Retrofit+OkHttp+Kotlinx Serialization；Room。minSdk 26，单 module。

必须遵守的硬约束（违反不可接受）：
1. 文言文防伪：课内22篇原文为本地固定语料，AI 绝不生成/改写课内原文；课外篇优先从本地课外库选；AI 只在库里没有时才提议课外篇，且必须带真实出处并打 verified=false/need_human_review=true，未人工确认不得入正式题库。现代文可由 AI 原创。
2. 批改踩点给分：每题输出逐采分点命中、错因、正确答法、一条可执行提升建议。
3. JSON 契约+校验：AI 返回按 SPEC §6 结构解析并校验；字段缺失或校验不过要拦截重试，绝不写脏数据入库。
4. API Key 安全：用户在设置页填，存 EncryptedSharedPreferences，禁止硬编码/日志/上传。
5. 计时与时间评价：答题页计时（切后台暂停），按 SPEC §7 的“速度×正确率”四象限给评价。
6. 轻量激励：仅 SPEC §8 的四个机制（目标卡/即时结算/徽章/连续打卡+等级），数值本地算。
7. DeepSeek：默认 deepseek-v4-pro 思考模式，做成设置页下拉；只取 message.content 作答案，忽略 reasoning_content；思考模式不传 temperature。

工作方式：严格按 SPEC §10 阶段推进，一次只做一个阶段，每阶段结束告诉我改了哪些文件、如何运行验证、下阶段计划，未经我确认不跨阶段。代码分包 data/domain/ui/core。关键模块写单测。拿不准的产品细节先问我，别擅自简化。

本段只做“阶段 0 脚手架”，完成标准：
1. 工程能用 `./gradlew assembleDebug` 编译通过（本机可能没装安卓SDK，请确保 Gradle wrapper 与依赖配置正确，能在标准环境编译；无需依赖 Android Studio）。一个空壳首页。
2. Room 建库：实现 SPEC §4 全部实体（含用时字段、UserProgress、Badge），DAO+Repository 骨架。
3. Retrofit 接通 DeepSeek（base_url=https://api.deepseek.com/）。
4. 设置页：输入并用 EncryptedSharedPreferences 安全保存 API Key + 模型下拉选项。
5. JSON 数据类与校验器占位 + 答题计时器基础设施。

先用 3-5 句复述你对硬约束和本段范围的理解，再动手。
```

---

## 〔第 2 段〕放入 7 个料文件

```
请把我提供的料文件放到位并接好：
- AiDtos.kt、AiResponseValidator.kt、DeepSeekApi.kt → data/ai/
- TimeEvaluator.kt → domain/
- kewen_seed.json、kewai_seed.json、exam_config.json → app/src/main/assets/seed/
- build-apk.yml → .github/workflows/（GitHub Actions 云端打包配置，阶段6会用）
然后用 DeepSeekApi.kt 里的 ModelOption（默认 PRO_THINK）和 ChatRequest.build(...) 接好出题/批改的网络调用；设置页模型下拉用 ModelOption 的四个选项，默认第一项。解析 AI 返回时先调 extractJson() 再反序列化，并取 ChatResponse.answer()。
```

---

## 〔第 3 段〕阶段 1 · 语料库

```
进入阶段1：实现 SeedLoader，首次启动把 assets/seed 的两个 json 导入 Room 的 TextBank；text 为空或 verified=false 的条目标为“待补全”，不可被出题选中。再实现 ExamConfigRepository 读取 exam_config.json，供命题约束与 TimeEvaluator 基准归一化使用。给 SeedLoader 补单测。
```
> 这一步之后，**你来补课内 17 篇原文**：打开 `kewen_seed.json`，把带 `_todo` 的条目按前 5 篇的格式补全 `text/notes/translation`、`verified` 改 true、删 `_todo`。

---

## 〔第 4 段〕阶段 2 · 出题闭环（两类）

```
进入阶段2：跑通“设置范围→AI出题→答题页并计时”。
1. 出题 UseCase：文言对比从课内库选甲、课外库选乙（均 verified=true）拼 user 消息；现代文走附录A 2.3。提示词用附录A。
2. 调 DeepSeekApi：模型/思考开关取设置页（默认 PRO_THINK）；extractJson 后反序列化为 GeneratedExercise，取 answer()。
3. 过 AiResponseValidator：errors→提示重试；needsReview→课外篇转“待确认”不入正式库。
4. 出题快照写 Exercise.payloadJson；进答题页启动计时（开始/切题/暂停/停止）。
5. 答题页逐题作答，文言题可回看原文。
完成标准：两类各能生成一套、过校验、可作答、记录每题用时。先给出题流程与提示词拼装，再实现。
```

---

## 〔第 5 段〕阶段 3 · 批改 + 时间评价

```
进入阶段3：踩点批改 + 结果页 + 时间评价。
1. 批改 UseCase：用附录A 2.4 提示词，user 填出题 questions 与学生作答；模型取设置页默认 PRO_THINK，取 answer()。
2. 解析为 GradingResult，过 AiResponseValidator.validateGrading(expectedIds=本次题目id集合)。
3. 写入 Attempt（got_score/point_check/error_type/explanation/tip/timeSpentSec）。
4. TimeEvaluator：从 exam_config.json 取该题型 sec_per_score，baselineRange(本次total_score)，evaluate(总用时,基准,正确率) 得四象限，合成 ExerciseSettlement。
5. 结果页：逐题采分点对照+错因+正确答法+提升建议+总分+薄弱点+用时与时间评价+最耗时题。
完成标准：提交后看到完整批改与时间评价并落库。
```

---

## 〔第 6 段〕阶段 4 · 进步系统 + 激励

```
进入阶段4：错题本、薄弱点、激励。
1. 批改后未满分题写 WrongQuestion（带 ability_tag）；更新 WeakPointStat(attempts/wrongCount/avgTimeSec)。
2. 错题本按考点归类，提供“针对薄弱点再出一套”（复用阶段2出题，侧重该薄弱 ability_tag）。
3. 薄弱点页展示近 N 次用时曲线。
4. 激励：练前目标卡；练后即时结算动画（XP进度条+积分）；按 TimeEvaluator 象限/里程碑发 Badge；首页显示 streakDays 与等级称号；更新 UserProgress。数值设置页可调、本地算。
完成标准：错题进本、薄弱点可二次练、激励四件套可见且持久化。补单测。
```

---

## 〔第 7 段〕阶段 5 · 加固与校准

```
进入阶段5：可靠性收尾。
1. AI 提议课外篇的人工确认流：列出 need_human_review 条目，确认后置 verified=true 方可入库。
2. exam_config.json 校准入口：拿到常州2026命题说明后按其 calibration_instruction 替换估计值。
3. 健壮性：DeepSeek 超时/重试/限流提示；JSON 解析失败兜底；断网时题库与错题本可用并友好提示；API Key 仅存 EncryptedSharedPreferences，不打印不上传。
4. 关键路径单测：出题/批改/校验/时间评价/激励。
完成标准：异常有兜底、可人工确认与校准、关键路径有测试。
```

---

## 〔第 8 段〕阶段 6 · GitHub Actions 云端打包 + 装机（交付）

```
进入阶段6：用 GitHub Actions 云端编译 APK，下载装进手机 + 本地备份。个人单机用，不上架，本机不装安卓SDK。
1. 应用名与图标：中文名（如“语文中考练”）+ 简单图标。
2. GitHub Actions：在 .github/workflows/ 下创建打包工作流（我已提供 build-apk.yml 草稿，请校对并对齐本项目的 Gradle 任务与产物路径）。要求：每次 push 到 main 自动跑 ./gradlew assembleDebug，用 debug 签名（个人自用，无需 keystore），把生成的 app-debug.apk 作为 artifact 上传，方便我在网页下载。
3. 装手机：从 GitHub 仓库 Actions 页面下载 artifact 里的 apk → 传到手机（网盘/微信/数据线）→ 手机允许“安装未知来源应用”后点开安装。
4. 首次检查：设置页填 Key、选 deepseek-v4-pro 思考模式，文言与现代文各跑通一次出题+批改。
5. 备份/恢复：导出/导入功能，把错题本、薄弱点、UserProgress 导出为本地 JSON，可导入恢复（不导出 API Key）。
完成标准：push 后 Actions 能自动产出可下载的 debug APK，手机能装、能填Key、能出题批改、能导出再导入。先校对 build-apk.yml 并说明从哪里下载 APK，再实现图标与备份。
```


---
---

# 附录 A · 出题/批改完整提示词（与 AiDtos.kt 字段严格对齐）

公共约束：只输出 JSON，不要 markdown、不要解释；key 一律 snake_case；各采分点分值合计=该题 max_score，各题 max_score 合计=total_score。

### A 2.1 出题 · 文言对比（库内选篇，type=wenyan_compare）
system：
```
你是常州市中考语文命题助手。严格遵守：
1. 课内外对比阅读，总分以用户给定 total_score 为准（默认15）。
2. 甲文(passage_in)、乙文(passage_out)都用用户提供的原文，禁止改动一字、禁止虚构替换；原样回填 title/author/dynasty/text/source_ref，并保留 verified 与 need_human_review。
3. 题型从{实词解释,虚词辨析,断句,句子翻译,内容理解,人物形象}组合，覆盖高频虚词(之/而/于/以)，初三中考难度、不超纲不偏怪。
4. 每题给 q_type, stem, ref_answer, score_points(逐点含 point 与 score), max_score, ability_tag；翻译题按关键词逐点给分。
5. 只输出如下结构 JSON（snake_case）：
{"type":"wenyan_compare","total_score":15,
 "passage_in":{"title":"","author":"","dynasty":"","text":"","notes":[],"source_ref":"","verified":true,"need_human_review":false},
 "passage_out":{"title":"","author":"","dynasty":"","text":"","notes":[],"source_ref":"","verified":true,"need_human_review":false},
 "questions":[{"id":"q1","q_type":"","stem":"","ref_answer":"","score_points":[{"point":"","score":1}],"max_score":4,"ability_tag":""}]}
```
user（App 填充）：
```
【甲·课内】{title}（{author}·{dynasty}）：{text}
【乙·课外】{title}（{author}·{dynasty}，出处：{source_ref}）：{text}
要求：total_score={N}，题量约{4}题，难度={基础/中等/偏难}，侧重考点={如 虚词之、句子翻译}。
```

### A 2.2 出题 · 文言（AI 提议课外篇·兜底）
在 2.1 system 基础上，当 user 未给乙文时追加：
```
若需你补充乙文：必须是真实存在的传世文献片段，注明 source_ref(书名+朝代+作者)；禁止原创/改写/仿写文言文；不能确认真实出处时，输出空 text 并将 need_human_review 置 true、verified 置 false，在 source_ref 写明原因。
```

### A 2.3 出题 · 现代文（type=xiandai）
system：
```
你是中考语文命题助手。生成一篇原创{记叙文/散文/说明文/议论文}短文（初三难度，约{800}字）并配题。题型覆盖{主旨概括,词句含义与作用,人物形象,写作手法及作用,结构思路,情感态度}中若干。每题给 q_type, stem, ref_answer, score_points, max_score, ability_tag。只输出 JSON：
{"type":"xiandai","total_score":{N},
 "passage":{"title":"","author":"","dynasty":"","text":"","notes":[],"source_ref":"原创","verified":true,"need_human_review":false},
 "questions":[{"id":"q1","q_type":"","stem":"","ref_answer":"","score_points":[{"point":"","score":2}],"max_score":4,"ability_tag":""}]}
```
user：`文体={...}，字数≈{...}，题量={...}，难度={...}，主题倾向={可选}。`

### A 2.4 批改（两类通用）
system：
```
你是常州中考语文阅卷老师，按“踩点给分”严格批改。对每题输出：
- got_score：依 score_points 命中给分，不超过 max_score；
- point_check：逐采分点{point,hit,score}，命中分合计应=got_score；
- error_type：错因，具体到知识点（如“误将‘之’判为代词”）；
- correct_answer：规范正确答法；
- explanation：为什么这样答、易错点；
- tip：一条可立即执行的提升建议，指向具体复习点。
最后给 total_got、total_full、weak_points、next_advice。只输出 JSON：
{"per_question":[{"id":"","got_score":0,"max_score":0,"point_check":[{"point":"","hit":true,"score":0}],"error_type":"","correct_answer":"","explanation":"","tip":""}],
 "total_got":0,"total_full":0,"weak_points":[],"next_advice":""}
```
user（App 填充）：
```
题目与参考答案：{出题返回的 questions 原样 JSON}
学生作答：[{"id":"q1","answer":"..."}, ...]
请按上述规则批改。
```

---

# 附录 B · DeepSeek 接入要点（已改好，速查）

- base_url `https://api.deepseek.com`，OpenAI 兼容 `/chat/completions`。
- 默认模型 `deepseek-v4-pro` 思考模式（设置页四选项下拉，默认第一项）。别用已下线的 deepseek-chat。
- 思考模式：`thinking={type:"enabled"}` + `reasoning_effort:"high"`；**temperature 在思考模式无效**，别依赖它。
- 返回里 `reasoning_content` 是思维链，**答案只取 `message.content`**（DeepSeekApi.kt 的 `answer()` 已封好）。
- 解析前先 `extractJson()` 清洗。
- 字段名 `thinking`/`reasoning_effort` 以 DeepSeek 官方 thinking_mode 文档为准，接入时核对一次。
